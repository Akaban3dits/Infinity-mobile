//referencia o nocliente, monto, fecha, concepto, institucion

class DataService{

  String referencia;
  DateTime fecha;
  String concepto;
  String institucion;

  DataService(this.referencia, this.concepto, this.fecha, this.institucion);
}

List<DataService> conceptos = [


];